SIDELINEIQ v0.40.6 — MOBILE STRUCTURAL STYLE FIX

The v0.40.5 screenshot proved the structural re-parenting occurred, but the
mobile workspace CSS was not being applied. The evidence: the live-game duplicate
logo was gone, but Offense / Special Teams / Field / Defense were still stacked
full-width and the field remained horizontal.

v0.40.6 fixes the actual cause:
- mobile workspace CSS keys directly from .mobile-score-workspace
- no dependency on the body mobile class or a media query for workspace geometry
- field orientation keys directly from .mobile-score-workspace
- topbar hiding keys from the existence of the structural workspace
- preserves structural left | field | right DOM from v0.40.5
- migrates v0.40.5 data
