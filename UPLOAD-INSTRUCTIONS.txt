SIDELINEIQ v0.40.4 — MOBILE LAYOUT ACTIVATION FIX

The v0.40.3 screenshot showed the old accordion mobile layout was still active.
Root issue: the deployed iPhone viewport was not satisfying the old <=760px
mobile-layout test, so the three-column DOM transformation never ran.

v0.40.4:
- activates the approved mobile layout through 900px viewport width
- makes the vertical-field renderer follow the activated mobile-layout class
- mobile header matches the prototype more closely
- mobile nav is Plays / Undo / Edit / Stats / Control
- removes Games and Drives from the mobile scoring header
- actual Offense + Special Teams are placed left of the field
- actual Defense + Penalty are placed right of the field
- field remains permanently centered/vertical
- retains v0.40.3 crash fix and all existing game workflows
- desktop remains unchanged above the mobile breakpoint
- migrates v0.40.3 data
