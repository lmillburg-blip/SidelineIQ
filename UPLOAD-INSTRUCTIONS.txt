SIDELINEIQ v0.40.3 — MOBILE CRASH HOTFIX

Root cause fixed:
v0.40.2 removes the desktop Recent Plays panel from the portrait-mobile DOM.
bindGame still tried to bind #undoPlay and #editPlay from that removed panel,
causing the live game screen to abort during initialization.

v0.40.3:
- guards the desktop-only Recent Plays action bindings
- keeps the dedicated mobile Undo button functional
- keeps the dedicated mobile Edit button functional
- preserves the v0.40.2 three-column / vertical-field mobile redesign
- preserves desktop behavior
- migrates v0.40.2 data
